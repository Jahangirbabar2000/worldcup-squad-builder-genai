# Team Contributions

This document summarizes the responsibilities and contributions of each team member for the World Cup Squad Builder project.

---

## Person A

_(To be filled by Person A)_

Suggested areas to document:

- Data sourcing and documentation (Kaggle dataset selection and `data/README.md`).
- Data ingestion and cleaning logic in `src/ingestion.py`.
- Document creation and embedding preparation.
- Retrieval pipeline design in `src/retrieval.py`.
- Initial drafts of prompt templates in `src/prompts.py`.

---

## Person B

_(To be filled by Person B)_

Suggested areas to document:

- Design of the reasoning pipeline in `src/reasoning.py`.
- Constraint logic for squad composition (max players, positional minimums, budget).
- LLM prompt engineering and validation strategies for grounded, stats-based justifications.
- Report generation design and formatting in `src/synthesis.py`.
- Handling of edge cases and retries when constraints are violated.

---

## Person C

_(To be filled by Person C)_

Suggested areas to document:

- LangChain tool wrapping in `src/tools.py`.
- Agent orchestration and memory configuration in `src/agent.py`.
- Gradio UI design and implementation in `app.py`.
- End-to-end pipeline demonstration and narrative in `notebook/pipeline.ipynb`.
- Final integration, repository organization, and submission packaging.

